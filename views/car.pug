extends layout

block content
  h1= title
  - let results = car_results
  div(class="container-fluid")
  div(class="row")
  each value,index in results
     div.col-sm-2(onclick=`buy(${index})`) car Name: #{item["car_Name"]} <br> car clour: #{item["car_clour"]} <br> car Size: #{item["car_Size"]}
  script.
    function buy(index)
    {
      alert(`Your purchased Item ${index+1}`)
    }